@model List<AnThinhPhat.ViewModel.Users.AddRoleInfoViewModel>
@for (int i = 0; i < Model.Count; i++)
{
    @Html.HiddenFor(x => Model[i].Id)
    @Html.DisplayFor(m => Model[i].Name)
    @Html.CheckBoxFor(x => Model[i].IsChecked)
}
